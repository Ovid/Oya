/**
 * Local storage configuration.
 *
 * All Oya storage is now consolidated in utils/storage.ts.
 * This file re-exports storage defaults for backwards compatibility.
 */

// Re-export from utils/storage for backwards compatibility
export { DEFAULT_STORAGE, DEFAULT_QA_SETTINGS } from '../utils/storage'
